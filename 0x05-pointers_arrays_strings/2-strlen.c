include "main.h"
/**
 * _strlen - prints the length of a string
 * @s: the string
 * Return: returns the length
 */
int _strlen(char *s)
{
	int len, count;

	len = 0;
	for (count = 0; s[count] != '\0'; count++)
		len++
			return (len);
}
